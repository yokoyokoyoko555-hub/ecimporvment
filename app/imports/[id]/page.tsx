import { notFound } from "next/navigation";import Link from "next/link";import { pool } from "@/lib/db";import { ProductEditor,type EditableProduct } from "./product-editor";import "./products.css";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){if(!pool)notFound();const{id}=await params;const br=await pool.query<{set_name:string;set_code:string|null;card_count:number;status:string}>("SELECT set_name,set_code,card_count,status FROM import_batches WHERE id=$1",[id]);if(!br.rows[0])notFound();const r=await pool.query<{id:string|null;source_key:string;card_number:string;card_name:string;rarity:string|null;colors:string[];is_parallel:boolean;source_image_url:string|null;product_code:string|null;product_name:string|null;sale_price:number|null;cost_price:number|null;initial_stock:number|null;department_id:string|null;category:string|null;export_enabled:boolean|null}>(`SELECT p.id,c.source_key,c.card_number,c.card_name,c.rarity,c.colors,c.is_parallel,c.source_image_url,p.product_code,p.product_name,p.sale_price,p.cost_price,p.initial_stock,p.department_id,p.category,p.export_enabled FROM cards c LEFT JOIN LATERAL(SELECT px.* FROM products px JOIN cards pc ON pc.id=px.card_id WHERE pc.card_number=c.card_number AND pc.variation_code=c.variation_code LIMIT 1)p ON true WHERE c.import_batch_id=$1 ORDER BY c.card_number,c.variation_code`,[id]);const products:EditableProduct[]=r.rows.map(x=>({id:x.id,sourceKey:x.source_key,cardNumber:x.card_number,cardName:x.card_name,rarity:x.rarity,colors:x.colors,isParallel:x.is_parallel,imageUrl:x.source_image_url,productCode:x.product_code||"",productName:x.product_name||x.card_name,salePrice:x.sale_price,costPrice:x.cost_price,initialStock:x.initial_stock||0,departmentId:x.department_id||"",category:x.category||"",exportEnabled:x.export_enabled??true}));const b=br.rows[0];return <><header className="topline"><div><div className="eyebrow">Import detail</div><h1>{b.set_name}</h1><p className="subtitle">{b.set_code}・{b.card_count}件・{b.status}</p></div><div className="headerActions"><Link className="button secondary" href="/imports">取込一覧へ</Link><Link className="button" href={`/exports?batch=${id}`}>出力へ</Link></div></header><ProductEditor initialProducts={products} batchId={id}/></>}
